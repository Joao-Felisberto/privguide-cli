PREFIX ex: <https://example.com/>

SELECT ?eId
WHERE {
    {
        ?_ ex:vital_interest ?eId
    } UNION {
        ?_ ex:legitimate_interest ?eId
    } 

    FILTER NOT EXISTS {
        ex:ROOT ex:external_entities ?eId 
    }
}

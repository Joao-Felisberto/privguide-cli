PREFIX ex: <https://example.com/>

SELECT ?s ?o
WHERE {
    ?s ex:validity|ex:periodicity ?o .
    FILTER NOT EXISTS {
        FILTER(
            REGEX(?o, "^[0-9]+(s|sec|secs|second|seconds|m|min|mins|minute|minutes|h|hr|hrs|hour|hours|d|day|days|w|week|weeks|month|months|y|year|years)$") || 
            ?o = "eternal"
        )
    }
}
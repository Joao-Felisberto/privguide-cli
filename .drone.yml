kind: pipeline
type: docker
name: default

steps:
- name: test
  image: localhost:5000/golang-fuseki:latest
  commands:
  - sleep 10s
  - ps aux
  - curl localhost:3030
  - go test ./...
  - go build

trigger:
  #branch:
  #  - master
  event:
    - push
